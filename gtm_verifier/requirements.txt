# Selenium 4.6+ ships with Selenium Manager, which auto-downloads chromedriver.
# No separate chromedriver installation is required as long as Chrome is installed.
selenium>=4.25.0
PyYAML>=6.0
python-pptx>=0.6.23

# Local web front end (webapp/)
Flask>=3.0

# Dev / tests
pytest>=7.0
