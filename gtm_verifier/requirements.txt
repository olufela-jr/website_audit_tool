# Playwright drives its own bundled Chromium — no system Chrome or chromedriver
# needed. After installing, download the browser once with:
#   python -m playwright install chromium
# Pinned exactly: the Dockerfile's base-image tag must ship the same version,
# so bump both together.
playwright==1.60.0
PyYAML>=6.0
python-pptx>=0.6.23

# seo.py parses HTML with lxml. It also arrives transitively via python-pptx,
# but webapp/app.py imports seo at module scope — so if python-pptx ever drops
# the dependency it breaks app startup, not just one audit. Pin it directly.
lxml>=5.0

# Web front end (webapp/): Flask app, served by gunicorn in the container.
Flask>=3.0
gunicorn>=21.2

# Google sign-in for the web front end (webapp/auth.py). Authlib needs an HTTP
# client for the OAuth token exchange and OIDC discovery; requests is its
# default for the Flask integration.
Authlib>=1.3
requests>=2.31

# Dev / tests
pytest>=7.0
