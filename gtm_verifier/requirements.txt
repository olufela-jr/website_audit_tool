# Playwright drives its own bundled Chromium — no system Chrome or chromedriver
# needed. After installing, download the browser once with:
#   python -m playwright install chromium
# Pinned exactly: the Dockerfile's base-image tag must ship the same version,
# so bump both together.
playwright==1.60.0
PyYAML>=6.0
python-pptx>=0.6.23

# Local web front end (webapp/)
Flask>=3.0

# Dev / tests
pytest>=7.0
