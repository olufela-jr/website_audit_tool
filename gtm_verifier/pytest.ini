[pytest]
markers =
    browser: launches a real (headless) browser — run explicitly with `pytest -m browser`
# Browser smoke tests are excluded by default; unit tests stay fast.
addopts = -m "not browser"
