================================================================================
GA4 / GTM AUDIT TOOL — COMPLETE AUDIT REFERENCE
================================================================================

Site:    https://palm-view-sparkling-water-co-948395720075.asia-south1.run.app
GTM ID:  GTM-K4BLC735
GA4 ID:  G-JXWM0TX7FS

Run command:
  python run.py                          # all audits
  python run.py <name> [<name> ...]      # specific audits only

Severity levels in output:
  [CRITICAL]  Compliance violation or tracking completely broken
  [HIGH]      Significant data quality or configuration issue
  [MEDIUM]    Implementation gap, informational risk
  [INFO]      Diagnostic output — does not count as a failure

================================================================================
INFRASTRUCTURE AUDITS (no selectors required — run these first)
================================================================================

--------------------------------------------------------------------------------
1. analytics_audit
   Command:  python run.py analytics_audit
   Source:   audit.py → run_analytics_audit()
--------------------------------------------------------------------------------

What it does:
  Opens BASE_URL in Chrome, accepts consent, then interrogates the page
  using three JavaScript executions only — no network proxy.

How each check works:

  GA4 presence
    Scans all <script> src/text and performance.getEntriesByType('resource')
    for the pattern G-[A-Z0-9]{6,}. Also polls resource timing for /collect
    URLs for up to 6 seconds to catch async hits.
    PASS = at least one measurement ID found OR a collect URL appeared.

  Single measurement ID
    Counts distinct G-XXXXXXX IDs found across all scripts and resources.
    FAIL = more than one ID detected (likely duplicate GA4 firing).

  Deployment method
    Priority order:
      1. GTM  — any script src containing "gtm.js", OR
                 window.google_tag_manager object present in JS runtime.
      2. gtag.js — script src containing "/gtag/js", OR
                   window.gtag function present (without GTM object).
      3. Hardcoded — gtag() call found in inline <script> text, no loader.
      4. Unknown — none of the above detected.
    Reports the active GTM container IDs from window.google_tag_manager keys
    (the live runtime object, more reliable than pattern-matching HTML).

  No conflicting implementations
    Checks whether BOTH a GTM loader (googletagmanager.com/gtm.js) AND a
    standalone gtag.js loader are present simultaneously.
    FAIL = both present — high risk of every GA4 event firing twice.

  First-party tagging
    Splits collect URLs and script sources into first-party (hostname is NOT
    google-analytics.com or googletagmanager.com) vs third-party.
    PASS = at least one first-party endpoint found.
    FAIL = all traffic goes directly to Google domains (standard setup,
           but no first-party durability / ITP resilience).

  sGTM / GTG signals
    Looks for: GTM loader served from a non-googletagmanager.com domain,
    collect endpoint on a non-google-analytics.com domain, and any resource
    URL containing "/gtg" or "gtg.json" (Google Tag Gateway).
    PASS = server-side tagging signals detected.
    FAIL = no signals (standard client-side only setup).

--------------------------------------------------------------------------------
2. consent_audit
   Command:  python run.py consent_audit
   Source:   consent.py → run_consent_audit()
   Requires: performance_logging driver (CDP network capture enabled)
--------------------------------------------------------------------------------

What it does:
  Opens BASE_URL WITHOUT clicking accept consent first. This is deliberate —
  pre-consent checks must run before any user interaction. Then accepts
  consent to verify the post-consent behaviour.

  Important: uses the network capture driver (performance_logging=True) so
  it can observe GA4 collect requests in addition to dataLayer events.

How each check works:

  CMP banner detected  [MEDIUM]
    Scans the DOM for visible elements matching known CMP vendor selectors
    in priority order:
      OneTrust, Cookiebot, TrustArc, Quantcast, Didomi,
      ClearCookie, Cookie Script, generic cookie/consent dialog patterns.
    Also checks window.__tcfapi (TCF 2.0 API — vendor-neutral signal).
    PASS = a matching visible element or __tcfapi was found.
    FAIL = no consent UI detected (consent management may be absent).

  Consent Mode implemented  [CRITICAL]
    Reads window.dataLayer and looks for a list-form entry matching:
      ['consent', 'default', { analytics_storage: '...', ... }]
    This is the gtag/GTM Consent Mode v2 default push that must appear
    before any GA4 tags fire.
    PASS = entry found.
    FAIL = not found — Consent Mode is not configured in GTM/gtag.

  Default denied: analytics_storage  [HIGH]
  Default denied: ad_storage          [HIGH]
  Default denied: ad_user_data        [HIGH]
  Default denied: ad_personalization  [HIGH]
    Each of the four Consent Mode v2 signals is checked individually
    against the consent default dict from the check above.
    PASS = signal value is exactly 'denied'.
    FAIL = signal is missing from the dict, or set to 'granted' by default.
    SKIP = only triggered if Consent Mode implemented check failed.

  Pre-consent GA4 firing  [CRITICAL]
    After page load but BEFORE clicking accept, polls the CDP performance
    log for GA4 collect requests (Network.requestWillBeSent events where
    the URL contains /collect and has v=2). Polls for up to 5 seconds.
    PASS = no collect requests observed before consent interaction.
    FAIL = collect request(s) fired — GA4 is tracking before consent.
           Reports which event names (en param) were sent.

  Post-consent update  [HIGH]
    Clicks the accept button (config.consent.accept_button selector).
    Then polls window.dataLayer for up to 8 seconds looking for any of
    these consent update event names:
      consent_update, gtm_consent_update, clearcookie_save_preferences,
      CookieInformationConsentGiven, cookieyes-consent-update
    Also handles the GTM nested push pattern: {"value": {"event": "..."}}
    If no dataLayer event is found, falls back to checking whether new
    collect requests appeared after the click.
    PASS (primary)  = a consent update event appeared in dataLayer.
    PASS (fallback) = collect requests fired post-consent (grant sent
                      directly via gtag without a dataLayer event).
    FAIL = no consent event and no collect requests — accept button
           selector is wrong or CMP is not integrated with GTM/GA4.

--------------------------------------------------------------------------------
3. network_audit
   Command:  python run.py network_audit
   Source:   network.py → run_network_audit()
   Requires: performance_logging driver (CDP network capture enabled)
--------------------------------------------------------------------------------

What it does:
  Opens BASE_URL, accepts consent, then captures all GA4 collect requests
  that fired during the session via Chrome DevTools Protocol performance
  logging. Parses each request's URL query params and POST body to extract
  event data, session identifiers, and consent state.

  The CDP log structure Selenium exposes is:
    {"message": {"method": "Network.requestWillBeSent", "params": {...}}}
  Only entries with method=Network.requestWillBeSent, URL containing
  /collect, and query param v=2 are included.
  POST body params (where GA4 sends event details) are merged in.

How each check works:

  GA4 collect requests  [CRITICAL]
    Polls the performance log for up to 10 seconds for any matching
    collect request. All remaining checks are skipped if none are found.
    PASS = at least one collect request observed.
    FAIL = no collect requests — GA4 may not be firing on this page.

  Client ID (cid)  [HIGH]
    Extracts the cid parameter from all collect requests.
    The Client ID identifies a unique browser/device across sessions.
    PASS = cid present; reports the actual value.
    FAIL = cid absent from all requests (identity stitching broken).

  Session ID (sid)  [HIGH]
    Extracts the sid parameter. Session ID groups events into a session.
    PASS = sid present; reports the actual value.
    FAIL = sid absent (session attribution will fail in GA4 reports).

  Consent state (gcs)  [HIGH]
    Extracts the gcs parameter which encodes the Consent Mode state at
    the time each request was sent.
    Format: G followed by digits — each digit is 1=granted or 0=denied
    for the signals: ad_storage, analytics_storage, ad_user_data,
    ad_personalization (in that order).
    Examples:
      G100 = all denied
      G101 = analytics_storage granted, others denied
      G111 = all granted
    PASS = gcs present; reports raw value and decoded interpretation.
    FAIL = gcs absent — Consent Mode signals not reaching collect calls.

  Event inventory  [INFO]
    Collects all distinct event names (en parameter) across every collect
    request. Reports count and full list.
    Always PASS — this is diagnostic output, not a gate.

  Session timeline  [INFO]
    Groups all collect requests by cid/sid combination and sorts each
    group by CDP timestamp to reconstruct the chronological event order
    within the session.
    Format: [cid=X / sid=Y]  event1 → event2 → event3
    Always PASS — diagnostic output.

================================================================================
DATALAYER JOURNEY AUDITS (require CSS selectors in config.yaml)
================================================================================

All journey audits below use window.dataLayer polling only — no network
capture. The tool polls dataLayer every 300ms until the expected event
appears or the timeout (15s by default) expires.

The after_index pattern: before every user interaction, the tool snapshots
len(window.dataLayer). The poll then only looks at entries AFTER that index,
so stale events from earlier in the journey are never matched.

--------------------------------------------------------------------------------
4. page_load
   Command:  python run.py page_load
   URL:      BASE_URL
   Selectors needed: consent.accept_button
--------------------------------------------------------------------------------

Actions:
  1. Navigate to BASE_URL
  2. Accept consent

Checks:
  init
    Fields: client_id, user_properties.customer_type
    Verifies the custom init event fires on page load with a client
    identifier and the customer type property populated.

  page_view
    Fields: page_title, page_path
    Verifies the standard page_view event fires with non-empty title
    and path (basic page tracking confirmation).

--------------------------------------------------------------------------------
5. consent
   Command:  python run.py consent
   URL:      BASE_URL
   Selectors needed: consent.accept_button
--------------------------------------------------------------------------------

Actions:
  1. Navigate to BASE_URL
  2. Snapshot dataLayer length (after_index)
  3. Accept consent

Checks:
  consent_update
    Fields: consent_choice
    The after_index ensures only the new push triggered by accepting
    consent is matched — not any pre-existing consent entries.
    Verifies the consent_update event fires and carries a consent_choice
    value indicating the user's selection.

--------------------------------------------------------------------------------
6. shop
   Command:  python run.py shop
   URL:      BASE_URL/shop
   Selectors needed: consent.accept_button, shop.product_card
--------------------------------------------------------------------------------

Actions:
  1. Navigate to /shop
  2. Accept consent
  3. Click first product card

Checks:
  view_item_list
    Fields: ecommerce.items
    Fires on page load. Verifies the product listing event fires and the
    items array is populated (not empty).

  select_item
    Fields: ecommerce.items.0
    Fires when a product card is clicked. Verifies at least one item
    object exists in the items array (dot notation: items[0]).

--------------------------------------------------------------------------------
7. product_detail
   Command:  python run.py product_detail
   URL:      BASE_URL/shop → clicks into PDP
   Selectors needed: consent.accept_button, shop.product_card,
                     product_detail.add_to_cart
--------------------------------------------------------------------------------

Actions:
  1. Navigate to /shop
  2. Accept consent
  3. Click product card (navigates to PDP)
  4. Click add-to-cart button

Checks:
  view_item
    Fields: ecommerce.value, ecommerce.items.0
    Fires on PDP load. Verifies the item value and at least one item
    in the items array are populated.

  add_to_cart
    Fields: ecommerce.value, ecommerce.items.0.quantity
    Fires when add-to-cart is clicked. Verifies value is populated and
    the first item has a quantity field.

--------------------------------------------------------------------------------
8. cart
   Command:  python run.py cart
   URL:      BASE_URL/shop → PDP → add to cart → /cart
   Selectors needed: consent.accept_button, shop.product_card,
                     product_detail.add_to_cart, cart.remove_button,
                     cart.proceed_to_checkout
--------------------------------------------------------------------------------

Actions:
  1. Navigate to /shop, accept consent
  2. Click product card, click add-to-cart (seeds the cart)
  3. Navigate to /cart
  4. Click remove button
  5. Navigate back to /shop, re-add item, navigate to /cart
  6. Click proceed-to-checkout

Checks:
  view_cart
    Fields: ecommerce.value, ecommerce.items
    Fires on /cart page load. Verifies cart total value and items array
    are both present and non-empty.

  remove_from_cart
    Fields: none required
    Fires when remove button is clicked. Presence only — no field
    requirements for this event.

  begin_checkout
    Fields: none required
    Fires when proceed-to-checkout is clicked. Presence only.
    Note: if the proceed button navigates to /checkout, the event must
    fire before navigation completes (standard GTM behaviour).

--------------------------------------------------------------------------------
9. checkout
   Command:  python run.py checkout
   URL:      BASE_URL/shop → add to cart → /checkout
   Selectors needed: consent.accept_button, shop.product_card,
                     product_detail.add_to_cart, checkout.address_field,
                     checkout.emirate_field
--------------------------------------------------------------------------------

Actions:
  1. Navigate to /shop, accept consent
  2. Click product card, click add-to-cart (seeds the cart)
  3. Navigate to /checkout
  4. Type "123 Test Street" into the address field
  5. Select the first option (index 1) from the emirate dropdown

Checks:
  add_shipping_info
    Fields: ecommerce.shipping_tier
    Fires when a shipping tier is selected. Verifies the shipping_tier
    parameter is populated (e.g. "Standard", "Express").

--------------------------------------------------------------------------------
10. purchase
    Command:  python run.py purchase
    URL:      ORDER_CONFIRMATION_URL (currently /order/1)
    Selectors needed: consent.accept_button
--------------------------------------------------------------------------------

Actions:
  1. Navigate directly to ORDER_CONFIRMATION_URL
  2. Accept consent

Checks:
  purchase
    Fields: ecommerce.transaction_id, ecommerce.value,
            ecommerce.shipping, ecommerce.items
    Verifies the purchase event fires on the confirmation page with all
    critical ecommerce fields populated.

  purchase.transaction_id format
    Validates the transaction_id value matches the pattern ^PV-\d+$
    (e.g. PV-1001, PV-42300).
    This is a separate named check so it appears as its own line in
    the report even if the purchase event itself passed.
    PASS = matches the pattern.
    FAIL = wrong format, empty, or purchase event was not found.

--------------------------------------------------------------------------------
11. subscribe
    Command:  python run.py subscribe
    URL:      BASE_URL/subscribe
    Selectors needed: consent.accept_button, subscribe.plan_card,
                      subscribe.form_email, subscribe.form_submit
--------------------------------------------------------------------------------

Actions:
  1. Navigate to /subscribe, accept consent
  2. Click a plan card
  3. Type test@example.com into the email field
  4. Click the subscribe form submit button

Checks:
  select_promotion
    Fields: none required
    Fires when a plan card is clicked. Presence only.

  generate_lead
    Fields: none required
    Fires when the subscribe form is submitted. Presence only.

  sign_up
    Status: SKIP — "Requires post-verification step"
    The sign_up event can only fire after the user clicks a link in a
    confirmation email. This cannot be automated and is always skipped.

--------------------------------------------------------------------------------
12. contact
    Command:  python run.py contact
    URL:      BASE_URL/contact
    Selectors needed: consent.accept_button, contact.first_field,
                      contact.submit_button, contact.name_field,
                      contact.email_field, contact.message_field
--------------------------------------------------------------------------------

Actions:
  1. Navigate to /contact, accept consent
  2. Click the first form field (focus trigger)
  3. Click submit without filling anything in (error trigger)
  4. Fill name="Test User", email="test@example.com",
     message="This is an automated verification message."
  5. Click submit again

Checks:
  form_start
    Fields: none required
    Fires when the first form field gains focus. Presence only.
    Triggered by clicking (not typing) the first field.

  form_submit_error
    Fields: error_fields, error_count
    Fires when an invalid/empty form is submitted. Verifies both the
    list of fields that failed validation and the error count are
    present in the event payload.

  form_submit
    Fields: none required
    Fires when a valid form is successfully submitted. Presence only.

--------------------------------------------------------------------------------
13. search
    Command:  python run.py search
    URL:      BASE_URL
    Selectors needed: consent.accept_button, search.input, search.submit
--------------------------------------------------------------------------------

Actions:
  1. Navigate to BASE_URL, accept consent
  2. Type "test product" into the search input
  3. Click the search submit button

Checks:
  search
    Fields: search_term, search_result_count
    Fires when a search is performed. Verifies both the term that was
    searched and the result count are present in the event payload.

================================================================================
SELECTOR CONFIGURATION (config.yaml)
================================================================================

All selectors below must be updated before running the journey audits.
Use browser DevTools (right-click element → Inspect → right-click in
Elements panel → Copy → Copy selector) to find the correct values.

Current status:
  consent.accept_button       = ".clearcookie-button--accept-all"  (CONFIRMED)
  shop.product_card           = ".product-card"                    (TODO)
  product_detail.add_to_cart  = "button.add-to-cart"               (TODO)
  cart.remove_button          = "button.remove-item"               (TODO)
  cart.proceed_to_checkout    = "button.checkout"                  (TODO)
  checkout.address_field      = "input[name='address']"            (TODO)
  checkout.emirate_field      = "select[name='emirate']"           (TODO)
  subscribe.plan_card         = ".plan-card"                       (TODO)
  subscribe.form_email        = "input[name='email']"              (TODO)
  subscribe.form_submit       = "button[type='submit'].subscribe"  (TODO)
  contact.first_field         = "input[name='name']"               (TODO)
  contact.name_field          = "input[name='name']"               (TODO)
  contact.email_field         = "input[name='email']"              (TODO)
  contact.message_field       = "textarea[name='message']"         (TODO)
  contact.submit_button       = "button[type='submit'].contact"    (TODO)
  search.input                = "input[type='search']"             (TODO)
  search.submit               = "button[type='submit'].search"     (TODO)

================================================================================
RECOMMENDED RUN ORDER
================================================================================

Step 1 — Infrastructure (no selectors needed):
  python run.py analytics_audit consent_audit network_audit

Step 2 — Core page tracking (only consent selector needed):
  python run.py page_load consent

Step 3 — Ecommerce funnel (requires shop/cart/checkout selectors):
  python run.py shop product_detail cart checkout purchase

Step 4 — Lead generation pages:
  python run.py subscribe contact search

Step 5 — Full run:
  python run.py

================================================================================
